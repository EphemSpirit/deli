<% flash.each do |message_type, msg| %>
  <div class="<%= message_type %> alert-dismissable fade show">
    <strong><%= msg %></strong>
    <button type="button class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
